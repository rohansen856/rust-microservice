#LINUX

```bash
sudo make container
```
- Then push the schema file into the DB

```bash
make run
```

#WINDOWS
- install [chocolatet](https://chocolatey.org/install)
- then run:
```bash
choco install make
sudo make container
```
- Then push the schema file into the DB

```bash
make run
```